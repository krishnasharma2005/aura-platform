# shared-types

Reserved for Phase 2: once the API contract stabilizes past the MVP, generate shared TypeScript types from the FastAPI OpenAPI schema here (e.g. via `openapi-typescript`) so `apps/web` stops hand-maintaining `lib/api-types.ts`. Not populated in Phase 1 — the frontend's hand-written types are the source of truth until the contract settles.
