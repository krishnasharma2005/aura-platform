# ui

Reserved for Phase 2: once `apps/mobile` exists, extract the shared design-system primitives from `apps/web/components` here so web and mobile render consistent components. Not populated in Phase 1 — there's only one client, so there's nothing to share yet.
